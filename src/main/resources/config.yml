# RaskolVault 1.0.0 — многовалютный экономический слой поверх EssentialsX
# для сервера «РАСКОЛ | ДВЕ КОРОНЫ»
#
# Фракции и столицы: Рассвет (Лайтрис) и Вальрадис (Драгос).
# Никакой семантики «свет/тьма» в коде нет — только точные ID наций Towny.

general:
  debug: false
  locale: ru
  # Резерв: интервал автосохранения кэша (записи в SQLite идут синхронно при мутациях)
  auto-save-minutes: 5

# Глобальная валюта: проксируется в EssentialsX (⚜)
global-currency:
  id: gold
  display-name: "Золото"
  symbol: "⚜"
  decimals: 2
  # Синхронизировать баланс gold с Essentials при старте и операциях
  essentials-sync: true

exchange:
  enabled: true
  # Комиссия по умолчанию для пары, если у пары нет своей в rates.yml (доля от суммы)
  default-fee: 0.02
  # Требовать явное подтверждение обмена командой /rv confirm
  require-confirm: true
  # Сколько секунд живёт ожидание подтверждения после /rv convert
  confirm-timeout-seconds: 30
  # Файл курсов обмена (формат ключей: <from>_<to>)
  rates-file: rates.yml
  # Резерв (этап 6): лимит обменов в секунду на игрока (анти-спам)
  rate-limit-per-second: 10

auto-convert:
  # Резерв: автоконвертация при /pay, если у плательщика другая валюта.
  # В 1.0.0 выключено: переводы идут только в валюте получателя.
  enabled: false
  require-confirm: true

hooks:
  essentials:
    enabled: true
  towny:
    enabled: true
    # Авто-создание национальной валюты при NewNationEvent
    auto-create-national: true
    # Точный символ на ID нации (lower-case от имени нации в Towny).
    # Рассвет — столица Лайтрис, Вальрадис — столица Драгос.
    # Ключи должны совпадать с именами наций в Towny (проверь /n list).
    nation-symbols:
      rassvet: "☀"
      valradis: "☾"
    # Символ для наций, не попавших в карту выше
    default-symbol: "¤"
  luckperms:
    enabled: true
  raskolcore:
    enabled: true
    # Регистрироваться провайдером в EconomyRegistry (вытесняет VaultEconomyProvider)
    register-as-provider: true
  placeholderapi:
    enabled: true

storage:
  sqlite:
    file: data/ledger.sqlite
    # Резерв: пул соединений (сейчас одно соединение + synchronized)
    pool-size: 5
  yaml-backup:
    enabled: true
    file: data/balances.yml
  daily-backup:
    # Резерв (этап 6): ежедневный бекап SQLite-файла
    enabled: true
    time: "04:00"
    keep-days: 7

audit:
  # Резерв: дублирование аудита в отдельный лог-файл с ротацией
  log-to-file: true
  log-to-console: false
  rotation-mb: 5

messages:
  file: messages.yml
